# Actions Required by Simo

2.  **Verify Booking Calculation**:al)**:
    *   If you have access to Supabase, check the `pricing_seasons` table to confirm `person_price_per_day` and other new columns are populated correctly.
